import React from 'react'
import { useEffect } from 'react'
import { useParams } from 'react-router-dom'

const Show = ({birdInfo, SetBirdInfo}) => {
  const {birdDisplay} = useParams();


  return (
    <div>

    </div>
  )
}

export default Show